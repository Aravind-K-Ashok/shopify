# Example environment variables for local development and deployment

# Full connection URI (preferred). Example for Aiven:
# DATABASE_URL=mysql://avnadmin:password@host:port/defaultdb?ssl-mode=REQUIRED
DATABASE_URL=

# Alternatively set individual values (used when DATABASE_URL is not set):
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ecomdb

# Optional: path to CA bundle inside the container/host to verify TLS (optional for local testing)
# DB_SSL_CA=/etc/ssl/certs/ca-certificates.crt
DB_SSL_CA=
